# ============================================================
# Grão de Solo — variáveis de ambiente
# ============================================================
# Frontend (Vite). O site funciona sem nada (o formulário cai para mailto).

# Formulário de contacto — Web3Forms (gratuito, sem backend).
# Gerar a key em https://web3forms.com (mete graodesolo@gmail.com → Create Access Key).
# Sem esta key, o formulário usa o fallback mailto.
VITE_WEB3FORMS_KEY=
